{
  "name": "snapcart",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build:shared": "npm run build --prefix shared",
    "build": "npm run build:shared && npm run build --prefix client && npm run build --prefix server"
  }
}

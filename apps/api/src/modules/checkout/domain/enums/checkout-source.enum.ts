/**
 * CheckoutSource Enum
 * Defines where the checkout is initiated from
 */
export enum CheckoutSource {
  PRODUCT = 'PRODUCT', // Buy now from product page
  CART = 'CART', // Checkout from cart
}
